# v15 入力整理と試験レイアウト

## 変更

- 体調とメモの重複区切り線を削除。タグ・日時の開閉と送信を横一列にした。
- 上の開閉ボタンの文言幅を固定し、下の矢印は展開時だけ表示する。
- 一枚（入力と閲覧の切替）、会話（下側に入力・履歴内スクロール）、日めくり（一日ごとの履歴と入力シート）を用意。
- 配置の定義を独立させ、共通の入力・保存・履歴を使う。UIの見た目と配置は別々に選択可能。
- 日めくりの空の下書きが、以前に開いた日付を持ち越す不具合を修正。内容がある下書きは日時も保持。
- 新たな統計は未実装。検討案を別文書に整理した。

## ブラウザー検証

- 管理プレビューを再起動して接続を復旧。390×844、320×640の同一オリジンのフレームで確認。
- 標準の区切り線がなくなり、タグ・日時と送信が横並びであることを画像で確認。
- 一枚の書く・読むの切替、上下のタグ閉鎖、下書きを会話へ持ち越して送信する一連の操作を確認。
- 会話で履歴が入力欄を押し潰す問題を発見し、flexの配分を修正。入力欄・下タブが収まることを確認。
- 試用領域にコメントだけの記録を追加し、履歴に体調3とコメントが現れることを確認。
- Apple風の日めくりで前日へ移動、空の入力シートを閉じて別日に再度開くとタイトル・保存日が追従することを確認。
- 過去日のコメントを送信し、その日の履歴と件数に反映されることを確認。
- 日付入力はonInputも扱うように修正。2026-09-18を指定し、見出しと日別件数が切り替わることを確認。日付フィールドのキー操作による月移動と、当日の記録だけへの絞り込みも確認。
- Apple風320pxで会話の入力、展開・下から閉じる、カレンダータブへの移動を確認。
- OpenAI風の一枚を320pxで確認。低い画面では試用説明を畳み、見出し・余白を縮小。
- ブラウザー上の幅検証であり、実機のソフトウェアキーボード・iOSのネイティブ日付選択の検証ではない。
- 実記録には書き込まず、試用領域だけで保存確認した。

## 自動検証

- 型検査、変更したTS/TSXのESLint、差分の空白検査が成功。
- 既存36テストがすべて成功（SQL、試用データ、統計、タグ開閉を含む）。
- 配備ワークフローでは、このソースの本番ビルドと配布物検証を必須にし、失敗時は反映を停止する。
- 検証専用のフレームHTMLは配布対象から削除。
