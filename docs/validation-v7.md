# 検証記録 — タグ一覧・波グラフ・試用モード

2026-09-19 UTC。対象はタグ数量入力、履歴表示、カレンダー、統計、Apple風UI。

## 自動確認

- Node.js 24で23テスト成功。実SQLiteで投稿・総括・タグ管理の保存、利用者分離、ページング、日時保持を確認。
- 120日分の生成データは565投稿・104総括。5段階、体調未選択、欠測日、小数数量、月境界、頻出タグの有無を含む。すべて入力スキーマに適合。
- 試用データ再投入は複製せず、実記録・他利用者に混入しない。
- 日平均のSQL結果を全日についてJavaScript集計と照合。体調未選択の投稿を3として含む。記録が存在しない日は補完しない。
- 型チェック成功。Lintはエラー0、既存テスト内の未使用変数警告1。
- Sites管理プロファイルの本番ビルド成功。検証用ルートは含めない。

## ブラウザー確認

管理プレビューの実D1で試用データ生成を実行。デスクトップと390×844の同一オリジンiframeで操作し、スクリーンショットを目視確認。検証用ページは公開前に削除。

- タグ追加→＋→＋→−で2、直接入力で0.5。別タグを外して再追加すると1に戻る。
- 送信前は履歴に増えず、送信後はコメント・体調3・タグ0.5と1を表示。
- 再読込後の編集で0.5に＋すると1.5。保存後も元の03:48を保持し履歴に反映。
- Apple風のスマホ入力、固定下タブ、カレンダー、日付編集ダイアログを目視確認。見出しの折り返しを修正。
- 色説明は「見方」で開き、閉じると通常表示に戻る。
- 月移動、総括／投稿平均切り替え、日別波の緑から赤の推移と欠測区間を確認。
- 密集したグラフの選択範囲の重複と、線がクリックを遮る問題を修正。8月16日のタップで同日の簡易統計・値5.0が選択されることを確認。キーボード操作も確認。
- カレンダーで8月1日をタップし総括2→3で保存。マス全体の色と選択状態が反映。
- 統計を90日に切り替えるとグラフ分母が90日、表示期間が6月23日〜9月20日に変わる。

実機Safariでの検証は未実施。試用データは架空の動作確認用であり、表示される関連は医学的な効果を表さない。
