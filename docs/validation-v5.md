# self-track-v5 検証記録

2026-09-18（日本時間）。基準ソースはSites初版`0e68c06c8e6c669651f444d79a04ebdcd884cffe`。PROMPTS/dev.md version 3、終了マーカー2.0.6の開発・Workbench規約を適用。GitHubの対象は新規空リポジトリ` tndd/self-track-v5 `。

## 変更範囲

送信確定、疼痛入力の撤去、編集時の時刻保持、独立した日別総括カレンダー、履歴の統合と自動追加読み込み、3スキン。これに必要なAPI・移行・集計の区別・CSV・文書・CIを含む。旧疼痛のDB列と既存マイグレーションは保全する。

## 必須の検証境界

ソース、Node、ロック済み依存、型検査、製品ビルド、実SQL、管理プレビューでの実ブラウザーとローカルD1、GitHub登録、Sites配備。Docker・外部医療APIは要件にない。Vercelはユーザー指定により任意。

## 自動検証

13件の計算・実SQL回帰テストが成功。

- 日本時間・日付境界・うるう日・月曜始まりのカレンダー。
- 未記録の除外、日を等重みにする集計、総括と時点記録の分離。
- 疼痛入力の拒否、未来日・不正な暦日・空投稿の拒否。
- 編集APIが日時を受け付けないこと。
- 実際のSQLiteと移行SQLで、編集後の日時（ミリ秒を含む）・日付・旧疼痛値を保持。
- 利用者ごとの分離、同じUUIDの再送を重複させない保存。
- 同時刻85件のページングを40・40・5件で取得し、重複・欠落なし。
- タグ・日付・文字列検索。`%`をワイルドカードとして扱わない。
- 日別総括の一意性と作成日時の保持、投稿との独立。
- CSVの記録種別・日時・改行の保持と式の無害化。

型検査・lint（エラー0、テスト内の未使用変数警告1件）・製品ビルドが成功。再開後にも13件のテストを再実行して成功。Reactのref更新をeffectへ移し、内部リンクをLinkへ統一。モバイル検証用iframeは撤去した。

## 実ブラウザー

検証環境のローカルD1だけに試験記録を作成。本番の利用者データは参照・変更していない。

- 体調4を選択しただけでは投稿数が変わらず、送信で初めて保存されることを確認。
- 過去日時の入力が送信に反映されない入力イベントの問題を検出。日時入力で`input`と`change`を扱うよう修正し、9月6日12:34の投稿に成功。
- その投稿の体調とメモを編集し、元の日時・履歴位置が保持されることを確認。
- 同じ日について、投稿の体調2と一日の総括5・コメントを独立して保存。再読み込み後も保持。
- 夜・紙・墨の切り替えを確認。スキンの設定はページの再読み込み後にも保持。
- 検証専用の390×844のiframeで、本物の小画面ビューポートとして記録・カレンダーを操作。検証用のページ変更は公開前に撤去。

## 公開境界

管理プレビューは実際のアプリとローカルD1を使う。製品の本番Sites URLはこのブラウザーから開けないため、公開の成功はSitesの正式な配備結果で確認する。ブラウザー拡張自身のログはアプリ不具合と区別する。

Vercelは接続済み一覧を取得できたが、配備操作がサーバーで未提供。現行のD1とSites認証の移植も未実施であり、Vercelで動作したとは扱わない。

## コンパクト入力・デザイン実験（2026-09-18）

- 5種類の顔アイコンを入力・履歴で共有。ラベルはアクセシビリティ名とツールチップに保持。送信で確定する仕様と保存APIは変更なし。
- 390×844の検証用iframeで標準入力の高さ265.4px（約31%）を測定。本文の横はみ出しなし。実際のアイコン選択→送信→履歴反映を確認。
- PCのワークスペース（左入力・右履歴）とノート（一列）、スマホの下部ナビを画像で確認。カレンダータブも遷移成功。
- 下書き入力後の配置切り替えでも内容を保持。別画面で再マウントした後の試験設定の復元を確認。
- 検証用ルートは削除し、本番の記録には操作していない。
- 再開後の型検査・13件の自動テスト・製品ビルドが成功。差分の空白検査も問題なし。

## タグ管理・数量・固定タブ（2026-09-18）

対象基準はSitesの `537b99b2ef178b382f50f1bad992f5e8271b9d60`（GitHubと同じツリー）。既存環境・依存を再利用し、型検査・実SQLite・管理プレビューで今回の変更を検証した。新しい依存や外部サービスは不要。

- タグ管理は利用者別D1に保存。所属グループ一つ、頻出上位6件は投稿回数順の派生表示。管理データの古い版による上書きを拒否。
- 新しい移行は管理テーブルの追加と数量JSON列の追加だけ。既存SQL・既存記録・時刻を保持。数量なしは1として扱う。
- 自動テスト18件が成功。数量の正の小数、未選択タグ・不正数値の拒否、保存・編集・再送、既存記録の既定値、頻度、所属・予約名、利用者分離、競合を確認。
- 実ブラウザーでグループと数量付きタグを作成。初期値1→2.5で保存、再読込で保持、編集で3に変更し履歴反映。頻出欄にも同じタグが現れる。
- メモのフォーカス解除時に高さが変わり送信クリックを取りこぼす問題を修正。入力直後の1クリック送信に成功。
- 日付から総括ポップアップを開き、評価4とコメントを保存。ポップアップが閉じ、同じ日を再選択して保存値を確認。
- PCの管理画面と日付ポップアップ、390×844のスマホ画面の固定4タブとポップアップを画像で視覚検査。スマホでもカレンダー・タグ管理への遷移と所属変更を実操作。
- 試験操作はローカル検証DBのみ。本番の個人記録は読まず変更しない。公開状態はSitesの配備結果で確認する。
- タグ非表示・再表示の導線を確認。検証専用ルートは撤去。最終の18件のテスト・型検査・製品ビルド成功、lintは既存の未使用変数警告1件のみ。

## モバイルUI統一・カレンダー・入力時数量・時間軸（2026-09-18 UTC）

- カレンダーは日付のみを体調アイコンと同じ色で表示。4〜6週の必要行だけを描画し、日別の総括・投稿平均・件数・タグを下に表示。
- 最近タグは利用者別の最終記録日時、頻出タグは投稿回数で分離。入力を開くとフォルダ見出しとタグ一覧を表示。
- タグ定義の数量設定を入力時の任意操作へ変更。既存の数量データ・DB構造は維持。
- 1時間以内の同日投稿をつなぎ、1時間超は線を切って間隔を表示。統計には全ページを取得する昇順の日別時間軸を追加。
- UI4種（標準・Apple風・Google風・OpenAI風）と配置4種（標準・手帳・時間軸・片手入力）を独立して保存。旧ノート・ワークスペースは撤去。
- 390×844で各UIと各配置を実操作。標準・Apple風手帳・Google風片手入力・OpenAI風時間軸のスクリーンショットを確認。全48配色組合せの総当たりではなく、共通実装と代表組合せを検証。
- 下書きメモと2.5の数量がUI・配置切替後も保持。事前設定のない睡眠タグで保存→履歴2.5→次回入力は1→編集3→履歴3を確認。最近タグの先頭にも反映。
- 統計タブで時間軸と数量を確認。前日8件は昇順、1時間超の差分あり。カレンダーの日付ポップアップと墨の角丸も確認。
- 入力シートの二重座標変換による画面外へのはみ出しを修正。修正後、同じ390×844で下部に全幅表示され送信できることを確認。
- PCの標準配置も目視確認。ブラウザーエラーログは拡張機能側のメタデータ送信エラーのみ。検証専用ルートは削除。
- 自動テスト20件成功。4/5/6週、1時間境界・日またぎ、最近順と頻度の相違・利用者分離を追加検証。型検査成功。Lintエラーなし（既存テストの未使用変数警告1件）。
