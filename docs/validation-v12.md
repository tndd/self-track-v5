# タグ開閉とApple風の再構成

## 対象と範囲

- 開始時のSites source: `4dfff4bb8896c1e8f858fa4a4306315acb6f1fd1`。
- 規約: `/PROMPTS/dev.md` version 3、終了マーカー `DEV_UNIFIED_END: 2.0.6`。Workbenchは同文書に統合されている。
- 最近タグの非表示、頻出タグの固定表示、上下の開閉操作、Apple風の専用スタイルへの再構成。
- 記録・集計・API・DB・アクセス範囲は変更しない。既存のUIと配置の選択肢も維持する。

## 受け入れ条件

1. 初期候補は頻度順の上位6タグ。最近タグは表示しない。
2. 上の同一ボタンで開閉。候補の折り返しとボタンの位置は展開で変わらず、グループ・日時だけが下へ伸びる。
3. 下にも矢印のみの開閉ボタン。閉じたときは上のボタンへフォーカスを戻し、必要な範囲だけスクロールする。
4. 下書き、選択タグ、数量、日時は開閉で初期化しない。
5. Apple風は黒系の背景・連続した日別記録面と、浮いたガラス風の操作層を区別。標準や他のUIにApple専用スタイルを漏らさない。

## 実行環境と検証境界

| 境界 | 結果 |
| --- | --- |
| Source・Runtime | 既存のclean checkout、Node 24.19.0、React 19.2.6のimport成功。依存追加なし、pnpm lockfile変更なし |
| 型チェック | 成功 |
| 既存テスト | 24件成功 |
| コンポーネント描画 | 実際のTagPickerをReact SSRで描画する4件を追加し成功。候補順、非表示、上下のaria-controlsとaria-expanded、展開領域との前後関係、数量を確認 |
| Build | 専用CSSを含む本番build成功 |
| ブラウザー | 管理プレビュー上で390pxのiframeに実アプリを表示し、初期状態の頻出タグのみ・上下ボタンのDOMを取得。設定の自然なクリックも実施したが、途中でプレビューがChromiumのblockedページへ変わり、直接のプレビューURLもERR_BLOCKED_BY_CLIENT。制限を迂回せず停止 |
| 最終目視QA | 未完了。Apple専用CSS適用後のスマホ画面、上下操作後のピクセル位置、カレンダー・統計・タグ管理・入力シートの最終画像は未検証。SSRやbuildを代わりの合格証拠にしない |
| 外部連携・データ移行 | 不要。今回は変更なし |

初回のCSS import追加直後にプレビューへ一時的な未解決importの表示が出た。ファイル作成後の本番buildで解決を確認したが、ブラウザー側の最終確認は上記制限により未回収。確認用HTMLはソースと配備物に残さない。

## デザイン根拠

[Appleのデザイン紹介](https://www.apple.com/newsroom/2025/06/apple-introduces-a-delightful-and-elegant-new-software-design/)が示す、内容とその上の操作層の区別を参考にした。ネイティブの屈折描画そのものは実装せず、Webの背景ぼかし・着色・縁の反射で表現する。読む面は不透明に近く保ち、低透明度・高コントラスト指定ではガラスの透過を抑える。

## 次の確認

管理ブラウザーの接続が回復したら、390px/320px/768pxで上下双方から開閉、長いタグ・数量保持、Apple風の全タブ、紙・墨、片手入力シートを実操作し、最終画像を目視確認する。現時点で完全検証済みとは扱わない。
