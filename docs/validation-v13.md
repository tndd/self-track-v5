# v13 検証記録

- 下側の矢印は展開中だけ表示。送信ボタンは非表示時も右端を保持。
- Apple風のスマホ用ダイアログは左右8px・下10pxを基準に配置。中央配置用のtranslate、transform、開閉アニメーションとの競合を除去。
- 実ブラウザー内390×780pxのフレームでApple風への切替と再表示を確認。選択ダイアログの矩形は x=8, y=10, width=374, height=760。画面外にはみ出さない。
- 実クリックで下側矢印の個数が閉状態0、展開1、下から閉じた後0になることを確認。
- タグ選択の描画テスト4件、型検査、本番ビルド成功。
- 検証用フレームは配布対象から削除。データ形式・API変更なし。
