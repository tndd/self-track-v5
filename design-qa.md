# モバイル中心のデザイン確認

final result: passed

## 対象と比較範囲

ユーザーの既存画面と今回の指示を正とする既存製品改修。生成モックは方向性の参考であり、画面複製ではない。UIと配置を独立させ、入力を約1/3画面に抑える指示を優先した。

- source visual truth: 会話中の既存しずく画面と3枚の生成案。シンプル案は `exec-5c432fa1-e56b-4b1d-997c-baae5877fc12.png`（853×1844）。
- implementation screenshots: 会話内のブラウザー画像「スマホの余白と丸みを確認」「手帳レイアウトのスマホ画面」「片手入力の位置を修正して確認」「時刻順と長い間隔の表示を確認」「墨のカレンダーも丸みを統一」「デザイン案と実画面を比較」。画像バイナリはリポジトリに含めない。
- viewport: 390×844 CSS px のiframe内に実アプリを表示し、その領域だけ390×844で撮影。PCは1363幅で確認。
- normalization: 生成案と実画面は同一スマホ縦横比。全体の構造・視覚言語を比較し、文字や余白は390 CSS pxの実画面を基準に判断。異なる記録内容と生成案の疑似データは比較対象外。
- full-view evidence: 最後のシンプル案とOpenAI風・標準配置を同じ比較入力に並べ、余白、面の抑制、アイコン色、丸い下部ドックを比較。入力を小さくするため、大きなアイコンや余分な説明を意図的に省略した。
- focused checks: 390pxの画像上でカレンダーの日付、数量入力、下部タブ、入力シートが読めるため追加拡大は不要。

## 必須確認面

- 書体: 既存の日本語システムフォントを維持。Apple風のみシステム書体優先。通常本文16px、入力16px、補助情報12〜14px。見出しの強弱と折り返しを確認。
- 間隔・配置: 標準の入力は約281px。下部ドックと画面余白を確保。手帳は日付と総括を上に、片手入力は履歴を主役に下から入力、時間軸は昇順。
- 色・トークン: 夜を基準に、各体調アイコンとカレンダー日付に共通変数を使用。墨でもカード・選択部・ダイアログは角丸。
- 画像・アイコン: 写真や装飾画像を必要としない記録UI。既存Lucideの顔・フォルダ・操作アイコンを使用。生成案の疑似チャート等は導入しない。
- 文言: UI実験と配置を分離。数量は今回の記録だけと明記。カレンダーの色は総括、投稿平均とは別。時間軸は距離が時間比例でないことを明記。

## 修正と再確認

- P1: 入力シートが半分画面外へ移動。既存ダイアログのtranslateと新しいtransformの重複を除去。再撮影で390px画面下に収まり、タグ・メモ・送信が操作できた。
- P2: 墨・紙の角丸指定が共通仕様と矛盾。専用の角を潰す指定を削除し、共通トークンに統一。墨カレンダーとポップアップを再撮影。
- P2: 時間軸の編集リンクが体調のみの行で本文と続いて見える。編集を独立行に変更。
- P2: 手帳の日付固定と履歴の解除表示が矛盾。手帳では日付をページ側で管理し、履歴の解除は検索語・タグだけに適用。

## 実操作・残る範囲

4UI・4配置それぞれを操作。下書き保持、数量2.5の保存、次回1、編集3、最近タグ、統計時間軸、前日移動、カレンダーポップアップを確認。PC表示も確認。エラーログはブラウザー拡張機能のメタデータ送信のみ。全UI×配置×配色の総当たり、実機のソフトキーボード・OS文字拡大は未検証。

未解消のP0/P1/P2なし。試験デザインは使用感のフィードバックを受けて調整する。
